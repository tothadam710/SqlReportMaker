Instructions:

With this simple tool you can define queries and export the a csv or a txt file.

1.Type the query into the textbox to define the statement.ű
2. Type the name of the PC or the IP address  where the SQL server has located into the machine name field.(if you keep it blank the local PC will be the location.)
3.Type the database name into the database field(you mustn't keep it blank)
4. And finally choose between the csv or the txt file.

!IMPORTANT!

In the source code there is a default number of fields.
The result depends on the sql statement and if your statement appears less fields the toll will fail with an sql error.

enjoy :)